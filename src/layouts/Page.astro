---
import Layout from './Default.astro';
import { MetaSEO } from '~/types';

export interface Props {
  meta?: MetaSEO;
  subtitle?: string;
}

const { meta, subtitle } = Astro.props;
---

<Layout {meta} subtitle={subtitle}>
  <slot />
</Layout>
